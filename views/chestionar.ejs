<% for(let i=0; i<intrebari.length; ++i) { %>
    <p><%= intrebari[i].intrebare %></p>
    <% for(let v of intrebari[i].variante){
        let c=0;%>
        <input type="radio" nume="q<%= i%>" value="<%= c++%>"/><%= v%><br/>
    <%
    }
    %>
<%
}
%>

<% /*
    Aici e ceea ce se va afla in header
*/ %>
<%- contentFor('header') %>
<h1 class="page-title">Bine ati venit</h1>

<form action="/rezultat-chestionar" method=""POST>
    
</form>

<h1>This is the footer</h1>
